#include <stdio.h>
#include "main.h"

/**
 * main - prints the name of the program
 * @argc: number count of the arguments
 * @argv: array of the arguments
 * Return: always 0
*/

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0])
}
